databases:
  - name: song-api-db
    plan: free

services:
  - type: web
    name: song-api
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: song-api-db
          property: connectionString
      - key: DB_HOST
        fromDatabase:
          name: song-api-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: song-api-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: song-api-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: song-api-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: song-api-db
          property: password
      - key: DDL_AUTO
        value: update
